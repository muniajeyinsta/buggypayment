'use strict';

/** Public API paths (for docs + 404 helper). */
const AVAILABLE_ROUTES = ['/', '/health', '/user/:uid', '/create-order', '/verify-payment', '/debug/users'];

module.exports = { AVAILABLE_ROUTES };
