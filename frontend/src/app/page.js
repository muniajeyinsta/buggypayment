import PaymentPage from './pay/page';

export const metadata = {
  title: 'Subscribe — EV Buggy',
  description: 'Mobile-first web payment for EV buggy subscriptions',
};

export default function Home() {
  return <PaymentPage />;
}
